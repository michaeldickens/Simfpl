def factorial(n) => (
	if (n == 0) 1 (n * (factorial (n - 1))))

def factorial(n) => {
	product = 1
	while (n > 0) {
		product *= n
		n -= 1
	}
}

def factorial(n) => (
	case n
		0 -> 1
		true -> factorial (n - 1))

def factorial(n) => ((1..n) fold ((x, y) => (x * y)))